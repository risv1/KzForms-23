import { FC } from 'react'
import EditComponent from '../components/EditComponent';
interface EditLayoutProps {};

const EditLayout: FC<EditLayoutProps> = ({}) => {
  return(
    <EditComponent />
  );
}

export default EditLayout;